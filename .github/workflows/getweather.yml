name: Get Weather

on:
    schedule:
        - cron: '0 10 * * *'
    workflow_dispatch:

jobs:
    run-weather-script:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Run weather script
              run: ./weather.sh